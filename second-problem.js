/* const wordLength = (text) => {
    const word = text.split(' ')
    const wordLength = []
    word.forEach(word => {
        wordLength.push(word.length)
    })
    if (wordLength % 2 == 0) {
        console.log("even");
    }
    else {
        console.log("odd");
    }
    return wordLength;
}
console.log(wordLength("Bizu"));

 */


